# Braille Vision — Environment Variables
# Copy: cp .env.example .env

# Groq — LLaMA OCR correction (optional)
# https://console.groq.com
GROQ_API_KEY=your_groq_api_key_here

# Local dev only (Render sets PORT automatically)
PORT=5050
